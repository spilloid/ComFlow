// lib/FFMpegReverseConverter.js
export class FFMpegReverseConverter {
    constructor(options = {}) {
        // e.g. options.format, etc.
    }

    async reverseConvert(audioBuffer) {
        // stub: reverse process for playback
        return audioBuffer;
    }
}
